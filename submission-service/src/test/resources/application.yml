spring:
  sleuth:
    enabled: false
  zipkin:
    enabled: false
  cloud:
    consul:
      enabled: false
    aws:
      credentials:
        access-key: "-"
        secret-key: "-"
      s3:
        region: eu-west-1
app:
  submission:
    bucket: x
    key-format: "models/{modelId}/submissions/{submissionId}/file/{filename}"
